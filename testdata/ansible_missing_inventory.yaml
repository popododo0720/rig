project:
  name: test-app
  language: go

source:
  platform: github
  repo: test/repo

ai:
  provider: anthropic
  model: claude-opus-4-6
  max_retry: 3

deploy:
  method: ansible
  config:
    playbook: site.yml
  timeout: 300s
