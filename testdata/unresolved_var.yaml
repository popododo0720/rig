project:
  name: test-app
  language: go

source:
  platform: github
  repo: test/repo
  token: ${UNDEFINED_VAR_THAT_DOES_NOT_EXIST}

ai:
  provider: anthropic
  model: claude-sonnet-4-20250514
  max_retry: 3

deploy:
  method: custom
  config:
    commands:
      - name: build
        run: "echo building"
        transport:
          type: local
  timeout: 300s
