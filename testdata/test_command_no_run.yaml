project:
  name: test-app
  language: go

source:
  platform: github
  repo: test/repo

ai:
  provider: anthropic
  model: claude-opus-4-6
  max_retry: 3

deploy:
  method: custom
  config:
    commands:
      - name: build
        run: "echo building"
        transport:
          type: local
  timeout: 300s

test:
  - type: command
    name: unit-test
    timeout: 120s
